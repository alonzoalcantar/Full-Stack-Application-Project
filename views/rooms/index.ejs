<!DOCTYPE html>
<html>

  <%- include('../partials/header'); %> 

  <body>
    <%- include('../partials/navbar'); %> 


    <h1>Room List</h1>
    <table class="table">
      <thead>
        <tr>
          <th>Delete</th>
          <th>Bed Size</th>
          <th>Room Package</th>
          <th>Non-Smoking</th>
          <th>Edit Room</th>
        </tr>
      </thead>
      <tbody>
        <!-- Write the line of EJS to iterate over movies using forEach -->
        <% rooms.forEach(r=> { %> 
          <tr>

            <td>
            <form action="/rooms/<%= r.id %>?_method=DELETE"
              class="delete-form" method="POST">
              <button type="submit">Delete</button>
            </form>
            </td>

            <td><%= r.bedsize %></td>
            <td><%= r.roompackage %></td>
            <td>
              <%= r.smoking %> 
            </td>

            <td><a href="/rooms/edit">Edit Room Type</a></td>

          </tr>

          

        <!-- Close the forEach using EJS here -->
        <% }); %> 
      </tbody>
    </table>